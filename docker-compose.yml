services:
  contas:
    container_name: contas
    restart: always
    build: 
      context: .
      dockerfile: dockerfile
    ports:
      - "${PORT}:${PORT}"
    expose:
      - "${PORT}"
    env_file:
      - .env
    volumes:
      - .:/app:rw